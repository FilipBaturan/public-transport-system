{
  "definitions": {},
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "http://public_transport_system/schemes/transportLine.json",
  "type": "object",
  "title": "Transport Line",
  "description": "Represents the transport line available in public transport system",
  "required": [
    "id",
    "name",
    "type",
    "positions",
    "schedule",
    "color",
    "width",
    "active",
    "zone"
  ],
  "properties": {
    "id": {
      "$id": "#/properties/id",
      "type": ["integer", "null"],
      "title": "Id",
      "description": "Transport line id",
      "default": 0,
      "examples": [
        1
      ],
      "minimum": 0,
      "multipleOf": 1
    },
    "name": {
      "$id": "#/properties/name",
      "type": "string",
      "title": "Name",
      "description": "Transport line name",
      "default": "",
      "maxLength": 30,
      "examples": [
        "test"
      ],
      "pattern": "^(.*)$"
    },
    "type": {
      "$id": "#/properties/type",
      "type": "string",
      "title": "Type",
      "description": "Transport line type",
      "default": "BUS",
      "enum": ["BUS", "METRO", "TRAM"],
      "examples": [
        "BUS", "METRO", "TRAM"
      ]
    },
    "positions": {
      "$id": "#/properties/positions",
      "type": "array",
      "minItems": 2,
      "title": "Stations",
      "description": "Transport line positions",
      "items": {
        "$id": "#/properties/stations/items",
        "type": "object",
        "title": "Station",
        "description": "Transport line station",
        "$ref": "http://localhost:8080/schemes/transportLinePosition.json"
      }
    },
    "schedule": {
      "$id": "#/properties/schedule",
      "type": "array",
      "title": "Schedule",
      "description": "Transport line schedule",
      "minItems": 0,
      "items": {
        "$id": "#/properties/schedule/items",
        "type": "object",
        "title": "Schedule item",
        "$ref": "http://localhost:8080/schemes/schedule.json"
      }
    },
    "color": {
      "$id": "#/properties/color",
      "type": "string",
      "title": "Color",
      "description": "Transport line color",
      "default": "blue",
      "enum": ["blue", "red", "brown", "purple", "black", "green"],
      "maxLength": 30,
      "examples": [
        "blue", "red", "brown", "purple", "black", "green"
      ]
    },
    "width": {
      "$id": "#/properties/width",
      "type": "string",
      "title": "Width",
      "description": "Transport line width",
      "default": "",
      "enum": ["", "w3", "w7", "w10"],
      "maxLength": 30,
      "examples": [
        "", "w3", "w7", "w10"
      ]
    },
    "active": {
      "$id": "#/properties/active",
      "type": "boolean",
      "title": "Active",
      "description": "indicates is transport line exist in database",
      "default": true,
      "examples": [
        true
      ]
    },
    "zone": {
      "$id": "#/properties/zone",
      "type": "integer",
      "title": "Zone",
      "description": "Transport line zone",
      "default": 0,
      "examples": [
        1
      ]
    }
  }
}
